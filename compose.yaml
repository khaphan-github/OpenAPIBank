services:
  postgres:
    container_name: postgres-db-open-banking
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=open-banking'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin@9192'
    ports:
      - '5432'
